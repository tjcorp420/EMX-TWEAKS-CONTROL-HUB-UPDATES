## EMX Tweaks Control Hub v1.0.1

This update cleans up the public update experience and improves the right-side layout.

- Hide raw GitHub update manifest links from the app UI.
- Change updates to a single branded button flow.
- Move Activity Log into its own Settings tab.
- Remove Activity from the right rail so System tools stay visible.
- Keep the EMX Tweaks setup wizard branding and installer assets.
